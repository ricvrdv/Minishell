
missing : 
signals (also broken pipe) leaks atm need to fi
check_single quotes fails in some cases; after export and echo  ???????
if unset path (avoid leaks);
invalid exit _code when using 3 pipes.

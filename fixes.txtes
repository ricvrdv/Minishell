echo $"user" ECHO $'USER = USER  (SHOULD EXPAND AND THEN REMOVE QUOTES AND JUST PRINT USER)

Unset path then minishell shoudl open 

<< eof > a cat ( fix)  << eof cat 

cat << 'e'of can't close with extra spaces


// valgrind --suppressions=readline.supp --leak-check=full -s --show-leak-kinds=all --track-fds=yes --show-below-main=no ./minishell 
